import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    input: {
        height: 40,
        borderColor: "gray",
        borderWidth: 1,
        paddingHorizontal: 8,
        marginTop: 4,
        borderRadius: 4,
    },
    error: {
        color: "red",
        marginTop: 4,
    },
});
