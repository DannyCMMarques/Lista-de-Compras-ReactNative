import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    containerPrincipal: {
        flex: 1,
        padding: 16,
    },
    scrollContent: {
        padding: 16
    },
    titulo:{
        marginTop:25,
        marginBottom:25,

    }
});
